# Task 08 Answer

为什么 Shell 中 `$var` 和 `"$var"` 有时会产生不同的结果?

因为不加引号的 `$var` 会经过"分词(word splitting)":Shell 会按空格/制表符把展开结果拆成多个词,还会做通配符展开;
而 `"$var"` 加了引号后表示"一个整体",不会被拆分。
所以当变量里存的是带空格的文件名(如 My Report.txt)时,`cp $file` 会把名字拆成 My 和 Report.txt 两个词而找不到文件,写成 `cp "$file"` 才能正确处理整个文件名。
